---
// NavItem.astro
interface Props {
  href: string;
  label: string;
}

const { href, label } = Astro.props;
---

<p class="group relative">
  <a href={href}>{label}</a>
  <span
    class="absolute -bottom-1 left-0 w-0 transition-all h-0.5 bg-indigo-600 group-hover:w-full"
  ></span>
</p>
